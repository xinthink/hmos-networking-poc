# Node
node_modules/

# TLS private keys (never commit)
mock-server/certs/key.pem

# HarmonyOS / DevEco build outputs
network-compare/**/build/
network-compare/**/.hvigor/
network-compare/**/.cxx/
network-compare/**/oh_modules/
network-compare/.idea/
network-compare/.hvigor/
network-compare/oh-package-lock.json5

# AI agent local dirs (installed by devecocli --project; DSH does NOT read them).
# Keep ONLY .agents/skills under version control (DSH project skills).
.claude-code/
.opencode/
.codex/
.claude/
.cursor/
.codebuddy/
.qoder/
.trae-cn/

# OS
.DS_Store
